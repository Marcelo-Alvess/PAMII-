<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="RevisaoXamarin.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="Welcome to Xamarin.Forms!" 
               x:Name="lblBoasVindas"
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand" />

        <Entry Text="{Binding Name}" Placeholder="Enter Name"></Entry>
        <Label Text="{Binding DisplayName}"></Label>

        <Label Text="{Binding DisplayMessage}" HorizontalOptions="Center" VerticalOptions="CenterAndExpand" />
        <Button Text="Show Message" Command="{Binding ShowCommand}" />

        <Button Text="Count Name" Command="{Binding CountCommand}" />
        
        <Button Text="Clear Texts" Command="{Binding CleanCommand}" />
    </StackLayout>

</ContentPage>
